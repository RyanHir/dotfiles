#! /bin/sh

if [ -n "$BASH_VERSION" ] && [ -f "$HOME/.bashrc" ]; then
    source "$HOME/.bashrc"
fi

export EDITOR=vim
export LANG=en_US.UTF-8
export LC_ALL="$LANG"
export TERM="xterm-256color"
export CHROME_EXECUTABLE="/usr/bin/chromium"
export NEWT_COLORS_FILE="$HOME/.config/whiptail.env"
