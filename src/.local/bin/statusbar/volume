#! /bin/sh

set -e

DEFAULT=$(pacmd stat | awk -F": " '/^Default sink name: /{print $2}')

VOL=$(pacmd list-sinks | awk ' \
    /^\s+name: /{indefault = $2 == "<'$DEFAULT'>"}
    /^\s+volume: / && indefault {print $5}')

echo "Volume: $VOL"