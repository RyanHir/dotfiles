#! /usr/bin/env bash

set -e
cd /sys/class/power_supply/BAT$id
[ "$(cat type)" = "Battery" ]

MAX=$(cat energy_full)
NOW=$(cat energy_now)
STATUS=$(cat status)
CHARGE=$(python3 -c "print(round($NOW / $MAX * 100, 2))")

echo "Batt $id: $CHARGE% $STATUS"
